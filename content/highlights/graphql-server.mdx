---
title: GraphQL Server
category: Free plugin
order: 100
video: /videos/video.mp4
videoDesc: We use the GraphiQL client to compose GraphQL queries, execute them against the server, and visualize their response. The GraphQL server processes the query, and gives a response with the requested data, in JSON format.
---

**GraphQL** is an API to retrieve exactly the data that you need, to power your WordPress themes, plugins, block and applications. Provide GraphQL query, and execute it against the **GraphQL server** to retrieve the needed data.
